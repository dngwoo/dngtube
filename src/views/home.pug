extends templates/layout
include mixins/videoBlock

block content
    .videos
        each video in videos
            +videoBlock({
                title: video.title,
                views: video.views,
                videoFile: video.fileUrl
            })